## Переменные окружения

```bash
APP_TITLE="Events Aggregator"
APP_DESCRIPTION="Special aggregator for Events Provider API"

CHECK_INTERVAL=60  # интервал проверки в минутах

ORIGINS=http://localhost,http://127.0.0.1  # список разрешенных источников, строка без пробелов через запятую
```